//today we will learn about variables.
const accountId = 131124; 
let accountEmail = "nachiket@gmail.com";
var accountPassword = "3231242dadsda";
accountCity = "Nevada";

//accountId = 2;   // not allowed
console.log(accountId);   

accountEmail = "dbajdad";
accountPassword = "najndkanfa";

console.table([accountId,accountEmail,accountPassword,accountCity]);  //you can also console log multiple varaiables into using tables.

/* if we want to store a constant variable in storage, only const will be used. Now, if we want a variable to storage, we can use let and var. let is much
better as it supports scope in JS. you cannot use one variable from one scope into another. therefore, let is better and use that only. */
